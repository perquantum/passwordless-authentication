config:
  selfUrl: # https://administration.passwordlessauthentication.dev
  corsOrigins: #
  connectionStrings:
    administrationService: #
    saasService: #
  authServer:
    authority: # http://passwordlessauthentication-st-authserver
    requireHttpsMetadata: "false"
    metadataAddress: # http://admin.passwordlessauthentication.dev/.well-known/openid-configuration
    swaggerClientId: WebGateway_Swagger
  remoteServices:
    abpIdentityBaseUrl: #
    useCurrentToken: "false"
  dotnetEnv: #
  redisHost: #
  rabbitmqHost: #
  elasticsearchUrl: #
  stringEncryptionDefaultPassPhrase: gsKnGZ041HLL4IM8
  AbpLicenseCode: #

synchedCommunication:
  authority: # https://admin.passwordlessauthentication.dev
  requireHttps: #
  validateIssuerName: #
  validateEndpoints: #

ingress:
  host: # passwordlessauthentication-st-administration
  tlsSecret: passwordlessauthentication-tls

image:
  repository: mycompanyname/passwordlessauthentication-service-administration
  tag: latest
  pullPolicy: IfNotPresent

env: {}
